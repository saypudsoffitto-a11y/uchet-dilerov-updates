(()=>{'use strict';const C=window.SyncCore8962,DEVICE='uchet_device_8962';let device;try{device=JSON.parse(localStorage.getItem(DEVICE)||'null')}catch(_){}if(!device?.id){device={id:crypto.randomUUID(),name:'Компьютер '+crypto.randomUUID().slice(0,4)};localStorage.setItem(DEVICE,JSON.stringify(device))}let busy=false,meta=null,online=false;const key=()=> 'uchet_sync_baseline_8962:'+String(syncCfg().url||''),recoveryKey=()=> 'uchet_before_master_8962:'+String(syncCfg().url||'');const baseline=()=>{try{return JSON.parse(localStorage.getItem(key())||'null')}catch(_){return null}};const status=t=>{const e=document.getElementById('syncStatus');if(e)e.textContent=t;paint()};function paint(){const role=meta?.masterId===device.id?'Главный':meta?.masterId?'Рабочий':'Роль не выбрана',b=document.getElementById('computerBadge8962');if(b)b.textContent=device.name+' · '+role+' · '+(online?'связь есть':'нет связи');const m=document.getElementById('masterName8962');if(m)m.textContent=meta?.masterId?'Главный: '+(meta.devices?.[meta.masterId]?.name||'компьютер'):'Выберите главный компьютер';const c=document.getElementById('claimMaster8962');if(c)c.hidden=!!meta?.masterId}
async function request(method,body){const r=await syncRequest(method,body);if(!r?.ok&&!r?.conflict){online=false;throw new Error(r?.message||'Нет ответа сервера')}if(r?.protocol!==2)throw new Error('Сервер требует обновления');meta=r.computers;online=true;return r}
const pending=()=>baseline()?C.diffOps(baseline().state.ops,state.ops):[];function productPatch(base,cur){const a=new Map((base.products||[]).map(p=>[C.id(p.id),p])),b=new Map((cur.products||[]).map(p=>[C.id(p.id),p]));return [...new Set([...a.keys(),...b.keys()])].filter(id=>!C.same(a.get(id),b.get(id))).map(id=>({id,before:a.get(id)||null,after:b.get(id)||null}))}function accept(r,changes,archives){const local=state,merged=C.applyTransaction(r.state||{},changes||[],archives);merged.sync={...local.sync,revision:r.revision};merged.update=local.update;merged.newmatros=local.newmatros;localStorage.setItem(key(),JSON.stringify({state:r.state,revision:r.revision,masterId:meta?.masterId}));localStorage.setItem(KEY,JSON.stringify(merged));state=norm(merged);render();paint()}
async function backup(){const data=JSON.stringify(state);localStorage.setItem(recoveryKey(),data);if(window.updateAPI?.saveBackup){const r=await window.updateAPI.saveBackup(data);if(!r?.ok)throw new Error('Не удалось создать резервную копию')}}
async function pull(manual){if(busy||!syncCfg().url)return false;busy=true;try{status('Загрузка с сервера…');const r=await request('GET');if(!meta?.masterId){status('Связь есть. Выберите главный компьютер.');return true}if(!baseline()){await backup();localStorage.setItem(recoveryKey(),JSON.stringify(state));accept(r,[],{});status('Успешно загружено · база '+r.revision+' · '+new Date().toLocaleTimeString('ru-RU'));return true}const changes=pending(),archives=C.diffArchives(baseline().state,state);accept(r,changes,archives);status('Успешно загружено · база '+r.revision+' · '+new Date().toLocaleTimeString('ru-RU'));return true}catch(e){online=false;status('Загрузка не выполнена: '+e.message);return false}finally{busy=false}}
async function push(manual){if(busy||!syncCfg().url||(!manual&&!syncCfg().enabled))return false;if(!baseline()){await pull(true);return false}busy=true;try{status('Отправка на сервер…');const base=baseline(),changes=pending(),archives=C.diffArchives(base.state,state),products=productPatch(base.state,state),groupsChanged=!C.same(base.state.groups||[],state.groups||[]),catalogChanged=!C.same(C.definitions(base.state),C.definitions(state));const r=await request('GET');if(!meta?.masterId)throw new Error('Выберите главный компьютер');const payload={protocol:2,action:'changes',device,baseRevision:r.revision,changes,archives};if(meta.masterId===device.id&&catalogChanged)payload.catalog=C.catalog(state);else if(products.length||groupsChanged)payload.catalogPatch={products,groups:groupsChanged?C.clone(state.groups||[]):[]};const result=await request('PUT',payload);if(result.conflict)throw new Error('База изменилась во время отправки. Повторите синхронизацию.');accept(result,[],{});status('Успешно отправлено · база '+result.revision+' · '+new Date().toLocaleTimeString('ru-RU'));return true}catch(e){online=false;status('Отправка не выполнена: '+e.message);return false}finally{busy=false}}
async function claim(){if(busy)return;busy=true;try{const r=await request('GET');if(meta?.masterId)throw new Error('Главный компьютер уже назначен');if(!confirm('Сделать этот компьютер главным? Перед отправкой будет создана резервная копия.'))return;await backup();const x=await request('PUT',{protocol:2,action:'claim',device,baseRevision:r.revision,state:C.clone(state)});localStorage.removeItem(key());accept(x,[],{});status('Этот компьютер — главный.')}catch(e){status(e.message);alert(e.message)}finally{busy=false}}
async function transfer(){alert('Передачу роли главного выполняйте после полной синхронизации всех компьютеров. В 8.9.69 автоматическая передача временно отключена для защиты базы.')}
const host=document.getElementById('sync');if(host&&!document.getElementById('computerBadge8962')){const box=document.createElement('div');box.className='card';box.innerHTML='<h3>Этот компьютер</h3><p id="masterName8962"></p><div class="actions"><button id="claimMaster8962" class="primary">Сделать главным</button></div><p class="muted">Карточки товаров синхронизируются точечно. При конфликте старая карточка не перезаписывает новую.</p>';host.prepend(box);document.getElementById('claimMaster8962').onclick=claim;const badge=document.createElement('button');badge.id='computerBadge8962';badge.className='secondary';badge.onclick=()=>go('sync');document.querySelector('header .actions')?.prepend(badge)}window.masterSync8962={pull,push,claim,transfer,device};paint();setTimeout(()=>{if(syncCfg().enabled&&syncCfg().url)pull(false)},500)})();