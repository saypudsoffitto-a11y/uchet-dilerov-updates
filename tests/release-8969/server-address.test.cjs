const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'../..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('8.9.69 migrates the obsolete Render URL to the Turso-backed service',()=>{const s=read('app/master-sync-8962.js');assert.match(s,/OLD_SERVER='https:\/\/uchet-dilerov-sync\.onrender\.com'/);assert.match(s,/TURSO_SERVER='https:\/\/uchet-dilerov-sync-8963-test\.onrender\.com'/);assert.match(s,/state\.sync\.url=TURSO_SERVER/);assert.match(s,/state\.sync\.revision=0/);});
test('server root and tolerant API routes are wired',()=>{const s=read('server/server.js');assert.match(s,/Сервер «Учёт дилеров» работает/);assert.match(s,/const route = requestPath\(req\)/);assert.match(s,/route === '\/health'/);assert.match(s,/route !== '\/api\/state'/);});
